local packages = {
    "brave-bin",
    "zed",
    "sbctl",
    "anydesk-bin",
    "brave-origin-nightly-bin",
}

return {
    description = "SazV can co",
    conflicts = {},
    packages = packages,
}
