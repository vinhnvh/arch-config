local packages = {
    "sbctl",
    "anydesk-bin",
    "zen-browser-bin",
    "onlyoffice-bin",
    "obsidian",
}

return {
    description = "SazV can co",
    conflicts = {},
    packages = packages,
}
