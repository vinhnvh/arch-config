local packages = {
    "brave-bin",
    "zed",
    "sbctl",
    "anydesk-bin",
    "teamviewer",
    "brave-origin-nightly-bin",
}

return {
    description = "SazV can co",
    conflicts = {},
    packages = packages,
}
